<div class="pages">
  <h2><%= paginate @posts %></h2>
</div>

<div id="container" class="">
  <% @posts.each do |post| %>
    <div class="post">
        <%= link_to image_tag(post.image.url(:thumb)), post_path(post) %>
    </div>
  <% end %>
</div>

<div class="pages">
  <h2><%= paginate @posts %></h2>
</div>